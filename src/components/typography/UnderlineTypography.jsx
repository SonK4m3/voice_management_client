import { styled, Typography } from "@mui/material";

const UnderlineTypography = styled(Typography)({
  fontWeight: "bold",
  borderBottom: "2px solid",
  paddingBottom: "4px",
});

export default UnderlineTypography;
